<h1>id = 
<%= @list.id %>
 のメッセージ詳細ページ</h1>

<table class="table table-bordered">
  <tr>
    <th>id</th>
    <td>
<%= @list.id %>
</td>
  </tr>
  <tr>
    <th>タイトル</th>
    <td>
<%= @list.status %>
</td>
  </tr>
  <tr>
    <th>メッセージ</th>
    <td>
<%= @list.content %>
</td>
  </tr>
</table>

<%= link_to 'このメッセージを編集する', edit_list_path(@list), class: 'btn btn-default' %>

<%= link_to 'このメッセージを削除する', @list, method: :delete, data: { confirm: '本当に削除してよろしいですか？' }, class: 'btn btn-danger' %>
